import 'package:flutter/material.dart';

class FixedFooterPage extends StatelessWidget {
  const FixedFooterPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
